# Runtime directory

Installable packages place the approved aarch64 WPE WebKit runtime here. The
runtime is intentionally absent from Git; source code alone is not playable.

Expected entry point: `runtime/run-moonrider.sh`.
